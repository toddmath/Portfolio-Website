import styled from 'styled-components';
import mixins from './mixins';

const Main = styled.main`
  ${mixins.sidePadding};
  /* padding: 0; */
  margin: 0 auto;
  /* width: 1302.17px; */
`;

export default Main;
